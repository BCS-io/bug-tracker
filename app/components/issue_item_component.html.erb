<div class="flex flex-col pb-1 border-t border-gray-700">
  <div class="pt-2">
    <div class="float-left mr-2"><%= render(WorkBadgeComponent.new(work: work)) %></div> <span class="leading-none text-gray-100"><%= summary %></span>
  </div>
  <div class="pt-1">
    <%= "##{id} opened #{created_ago} by #{username}" %>
    <%= status %>
  </div>
</div>
